# AGENTS.md

## 仓库定位

这是公开简历的单一内容源仓库。正式简历事实、技能描述、工作经历和项目摘要维护在 `content/`，构建及渲染代码维护在仓库其他目录。

本仓库不维护详细的个人复习笔记、面试讲解、故障案例或内部项目架构。这些资料统一放在私人 Second Brain 中。

## 关键路径

- 正式简历内容：`./content/`
- 项目事实底稿：`./content/projects/`
- 简历变体：`./content/variants/`
- 私人复习资料：`~/Documents/Obsidian/Second-Brain/30-Areas/个人/简历/`
- 复习统一入口：`~/Documents/Obsidian/Second-Brain/30-Areas/个人/简历/00-复习入口.md`
- 当前有效的工作项目代码根目录：`~/Documents/work/`
- 历史迁移备份：`~/Documents/work-migration-20260805/`

详细的内部项目目录映射只记录在私人复习入口中，不写入本公开仓库。默认使用 `~/Documents/work/` 下的有效目录，不要把历史迁移备份当成当前项目。

## 处理简历或复习任务前

1. 先读取 `content/profile.yaml`、`content/skills.yaml`、`content/experience.yaml` 和相关 `content/projects/*.md`，确认简历当前实际写法。
2. 再读取 Second Brain 的 `00-复习入口.md`，找到对应的项目面试稿和真实代码目录。
3. 需要核实项目事实时，只读检查真实项目代码、提交记录和现有文档，不要仅根据简历摘要推断实现细节。
4. 区分“本人直接负责”“参与联调与排障”“团队整体完成”，不要扩大职责边界。

## 内容归属

- 简历事实发生变化时，修改本仓库 `content/`。
- 复习清单、面试回答、项目讲解和故障复盘发生变化时，修改 Second Brain。
- 不在本仓库重新创建详细复习稿，避免形成两个可编辑副本。
- 如简历改动影响已有复习资料，应指出需要同步检查的 Second Brain 笔记；只有用户明确授权时才写入。

## 安全边界

- 本仓库按可能公开发布处理，不写入客户信息、凭据、内网地址、私有仓库地址或未公开实现细节。
- 私人复习资料可以记录项目目录和职责证据，但仍不得记录密码、Token、Cookie、私钥等敏感信息。
- 如果上述路径不存在，先在当前主机的 `~/Documents/` 下只读定位，不要创建软链接或猜测替代目录。
